import { CookieTexturePipe } from './cookie-texture.pipe';

describe('CookieTexturePipe', () => {
  it('create an instance', () => {
    const pipe = new CookieTexturePipe();
    expect(pipe).toBeTruthy();
  });
});
